# Smart Factory MES System 问题修复记录

## 修复时间

2026-04-10

## 修复的问题

### 1. 数据库表缺少字段

**问题描述**: 多个业务表缺少 `deleted_time` 和 `deleted_by` 字段，导致逻辑删除时出错。

**受影响表**:
- `wo_work_order` (工单表)
- `pt_process_template` (工艺模板表)
- `pt_process_parameter` (工艺参数表)
- `qms_quality_record` (质检记录表)

**解决方案**: 执行 ALTER TABLE 语句添加缺失字段：

```sql
ALTER TABLE wo_work_order ADD COLUMN deleted_time DATETIME DEFAULT NULL;
ALTER TABLE wo_work_order ADD COLUMN deleted_by BIGINT DEFAULT NULL;
-- 其他表类似
```

---

### 2. 前端表单字段名与后端 DTO 不匹配

**问题描述**: 前端表单的字段名与后端 CreateDTO 的字段名不一致，导致创建失败。

**受影响字段 (工单)**:
| 前端字段 | 后端字段 |
|---------|---------|
| code | orderNo |
| name | productName |
| quantity | planQuantity |

**受影响字段 (工艺模板)**:
| 前端字段 | 后端字段 |
|---------|---------|
| code | templateNo |
| name | templateName |

**解决方案**: 修改前端 `WorkOrderView.vue` 和 `ProcessView.vue` 中的表单字段名。

---

### 3. 日期格式无法解析

**问题描述**: 前端发送的日期字符串格式 "2026-03-30:00" 无法被后端 LocalDateTime 解析。

**错误信息**: `Invalid format: "2026-03-30:00"`

**解决方案**: 
- 移除创建请求中的日期字段，让后端自动填充
- 或使用 ISO 格式 "2026-03-30T00:00:00"

---

### 4. 后端 DTO 验证注解导致 400 错误

**问题描述**: CreateWorkOrderDTO 使用了 `@NotBlank` 等验证注解，在字段名不匹配时直接返回 400 错误，无法看到真实错误原因。

**解决方案**: 临时移除 DTO 的 `@Valid` 注解。

---

### 5. JavaScript Long ID 精度丢失导致删除失败 (核心问题)

**问题描述**: MyBatis-Plus 使用 Snowflake 算法生成的 ID 超过 JavaScript Number 的安全整数范围 (2^53-1)，导致 ID 在前后端传递时精度丢失。

**示例**:
- 数据库 ID: `2042490503953403905`
- JavaScript 接收: `2042490503953404000` (精度丢失)

**错误现象**: 删除时查询不到记录 ("工单不存在")

**解决方案**:

#### 5.1 后端序列化时将 Long 转 String

在 `BaseEntity.java` 中添加 `@JsonSerialize` 注解：

```java
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.ser.std.ToStringSerializer;

@TableId(type = IdType.ASSIGN_ID)
@JsonSerialize(using = ToStringSerializer.class)
private Long id;
```

#### 5.2 前端类型修改

修改 API 函数接受 string 或 number：

```typescript
// services.ts
export function deleteWorkOrder(id: string | number) {
  return request({ url: `/workorder/${id}`, method: 'delete' })
}
```

---

### 6. 乐观锁 @Version 导致更新失败

**问题描述**: 手动调用 `updateById()` 时，如果实体类使用了 `@Version` 注解，MyBatis-Plus 会自动注入乐观锁条件，但参数映射缺失导致 500 错误。

**错误信息**: `Parameter 'MP_OPTLOCK_VERSION_ORIGINAL' not found`

**解决方案**: 使用 `UpdateWrapper` 代替 `updateById()`：

```java
// 错误的写法
order.setDeleted(1);
updateById(order);  // 会触发 @Version 检查

// 正确的写法
var updateWrapper = new UpdateWrapper<WorkOrder>()
        .set("deleted", 1)
        .set("deleted_time", LocalDateTime.now())
        .set("deleted_by", userId)
        .eq("id", id);
mapper.update(null, updateWrapper);
```

---

### 7. 质量管理模块 - 创建和删除功能

**问题描述**: 质量管理模块缺少完整的创建表单和删除功能。

**受影响文件**:
- `mes-quality/.../QualityService.java` - 添加 deleteRecord 方法
- `mes-quality/.../QualityServiceImpl.java` - 实现删除逻辑
- `mes-quality/.../QualityController.java` - 添加 DELETE 接口
- `mes-quality/.../QualityRecord.java` - 添加 ToStringSerializer
- `mes-frontend/.../services.ts` - 添加 createQualityRecord/deleteQualityRecord
- `mes-frontend/.../quality.ts` - 添加 API 函数
- `mes-frontend/.../QualityView.vue` - 添加创建表单和删除按钮

**解决方案**:
1. 后端添加删除接口，使用 UpdateWrapper 避免乐观锁问题
2. 前端添加完整的创建对话框表单
3. 修复前后端字段名匹配 (result → checkResult, workOrderCode → workOrderNo)

---

## 文件修改清单

| 文件路径 | 修改内容 |
|---------|---------|
| `mes-common/.../BaseEntity.java` | 添加 ToStringSerializer |
| `mes-frontend/.../services.ts` | 接受 string 类型 ID |
| `mes-frontend/.../workorder.ts` | 接受 string 类型 ID |
| `mes-frontend/.../WorkOrderView.vue` | 修改表单字段名 |
| `mes-frontend/.../ProcessView.vue` | 修改表单字段名 |
| `mes-workorder/.../WorkOrderServiceImpl.java` | 使用 UpdateWrapper |
| `mes-process/.../ProcessTemplateServiceImpl.java` | 使用 UpdateWrapper |
| `mes-quality/.../QualityService.java` | 添加 deleteRecord 方法 |
| `mes-quality/.../QualityServiceImpl.java` | 实现删除逻辑 |
| `mes-quality/.../QualityController.java` | 添加 DELETE 接口 |
| `mes-quality/.../QualityRecord.java` | 添加 ToStringSerializer |
| `mes-frontend/.../QualityView.vue` | 添加创建表单和删除按钮 |

---

## 总结

本次修复解决了以下问题：
1. 数据库表结构不完整
2. 前后端字段名不一致
3. 日期格式问题
4. 验证注解干扰
5. JavaScript 大数精度丢失 (核心)
6. 乐观锁更新失败 (核心)
7. 质量管理创建/删除功能

---

## 2026-04-13 修复

### 8. Jackson LocalDateTime 序列化失败

**问题描述**: WebSocket 广播数据时，`java.time.LocalDateTime` 类型无法被 Jackson 序列化。

**错误信息**: `Java 8 date/time type java.time.LocalDateTime not supported by default`

**解决方案**:
1. 添加依赖 `jackson-datatype-jsr310`
2. ObjectMapper 注册 JavaTimeModule 并禁用时间戳格式

```java
objectMapper.registerModule(new JavaTimeModule());
objectMapper.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS);
```

**受影响文件**:
- `mes-dashboard/pom.xml` - 添加依赖
- `mes-dashboard/.../DashboardWebSocketHandler.java` - 配置 ObjectMapper

---

### 9. 逻辑删除与唯一索引冲突

**问题描述**: 唯一索引只包含业务字段（如 `template_code`），逻辑删除时设置 `deleted=1` 会与已删除记录冲突。

**错误信息**: `Duplicate entry 'xxx-1' for key 'uk_template_code'`

**解决方案**: 将 `deleted` 字段加入唯一索引

```sql
ALTER TABLE proc_template DROP INDEX uk_template_code;
ALTER TABLE proc_template ADD UNIQUE KEY uk_template_code (template_code, deleted);
```

**受影响表**:
- `proc_template` - 工艺模板
- `wo_work_order` - 工单
- `qms_quality_record` - 质检记录

---

### 10. 质量记录创建失败

**问题描述**: `work_order_id` 字段没有默认值且 NOT NULL，创建时未传值导致失败。

**解决方案**:
1. 修改数据库字段允许为空
2. DTO 添加可选字段

```sql
ALTER TABLE qms_quality_record MODIFY COLUMN work_order_id bigint DEFAULT NULL;
ALTER TABLE qms_quality_record MODIFY COLUMN work_order_no varchar(50) DEFAULT NULL;
```

**受影响文件**:
- `mes-quality/.../CreateQualityRecordDTO.java` - 添加可选字段
- `mes-quality/.../QualityServiceImpl.java` - 设置所有字段

---

### 11. 工单状态流程修复

**问题描述**:
- 工单状态流转逻辑正确，但前端"开始"按钮在 CREATED 状态也显示
- 直接点击"开始"导致"不允许从已创建转换到生产中"错误

**解决方案**:
1. 前端"开始"按钮只显示在 ISSUED 状态
2. 添加后端错误消息提示

**受影响文件**:
- `mes-frontend/.../WorkOrderView.vue` - 修复按钮显示逻辑

---

### 12. 添加工单删除功能

**解决方案**:
1. 后端已有 delete 方法（WorkOrderController）
2. 前端添删除按钮和确认对话框

**受影响文件**:
- `mes-frontend/.../WorkOrderView.vue` - 添加删除功能

---

### 13. 全局异常处理器优化

**问题描述**:
- 所有错误返回 Spring 默认格式 `{"timestamp":"...", "status":500, "error":"Internal Server Error"}`
- 无法看到具体错误信息

**解决方案**:
1. 所有微服务添加 `server.error` 配置启用详细错误信息
2. 添加 `@ComponentScan` 确保 GlobalExceptionHandler 被扫描
3. 增强异常处理，添加 `@ResponseStatus` 和更多异常类型处理

**受影响文件**:
- `mes-auth/.../application.yml` - 添加 error 配置
- `mes-workorder/.../application.yml` - 添加 error 配置
- `mes-process/.../application.yml` - 添加 error 配置
- `mes-quality/.../application.yml` - 添加 error 配置
- `mes-dashboard/.../application.yml` - 添加 error 配置
- `mes-*/.../Application.java` - 添加 @ComponentScan
- `mes-common/.../GlobalExceptionHandler.java` - 增强异常处理

---

### 14. 移除 @Version 乐观锁

**问题描述**:
- 报工时出现 `Parameter 'MP_OPTLOCK_VERSION_ORIGINAL' not found` 错误
- 数据库没有 version 字段但代码使用了 @Version 注解

**解决方案**:
1. 移除 BaseEntity 中的 @Version 注解
2. 执行 SQL 删除数据库 version 列

**受影响文件**:
- `mes-common/.../BaseEntity.java` - 移除 @Version
- `sql/fix_version.sql` - 删除 version 列

---

### 15. 数据库表缺失字段

**问题描述**:
- sys_menu, sys_user 等表缺少 deleted_time, deleted_by, version 字段

**解决方案**:
添加缺失字段

**执行 SQL**:
```sql
ALTER TABLE sys_menu ADD COLUMN deleted_time DATETIME DEFAULT NULL;
ALTER TABLE sys_menu ADD COLUMN deleted_by BIGINT DEFAULT NULL;
ALTER TABLE sys_user ADD COLUMN deleted_time DATETIME DEFAULT NULL;
ALTER TABLE sys_user ADD COLUMN deleted_by BIGINT DEFAULT NULL;
```