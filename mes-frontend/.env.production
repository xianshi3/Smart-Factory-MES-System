# 生产环境 API 基础路径（走网关 /api/**，需在 Nginx/网关层将 /api 转发到 mes-gateway:9090）
VITE_API_BASE_URL=/api

# AI 服务地址：与网关路由 /api/ai/** 对应（StripPrefix=2 后转发到 AI 服务）
VITE_AI_SERVICE_URL=/api/ai

# WebSocket 地址（可选）：默认自动推导为同源 /api/ws/dashboard（网关 WebSocket 路由）
# 若 Nginx 未代理 WS，可显式配置，例如：VITE_WS_URL=wss://mes.example.com/api/ws/dashboard
# VITE_WS_URL=
