server:
  port: ${SERVER_PORT:9090}
  shutdown: graceful

spring:
  application:
    name: mes-gateway
  lifecycle:
    timeout-per-shutdown-phase: 30s
  cloud:
    compatibility-verifier:
      enabled: false
    gateway:
      routes:
        - id: mes-auth
          uri: ${MES_AUTH_URI:http://localhost:8081}
          predicates:
            - Path=/api/auth/**
          filters:
            - StripPrefix=1
        - id: mes-workorder
          uri: ${MES_WORKORDER_URI:http://localhost:8082}
          predicates:
            - Path=/api/workorder/**
          filters:
            - StripPrefix=1
        - id: mes-process
          uri: ${MES_PROCESS_URI:http://localhost:8083}
          predicates:
            - Path=/api/process/**
          filters:
            - StripPrefix=1
        - id: mes-quality
          uri: ${MES_QUALITY_URI:http://localhost:8084}
          predicates:
            - Path=/api/quality/**
          filters:
            - StripPrefix=1
        - id: mes-dashboard
          uri: ${MES_DASHBOARD_URI:http://localhost:8085}
          predicates:
            - Path=/api/dashboard/**
          filters:
            - StripPrefix=1
        - id: mes-alarm
          uri: ${MES_DASHBOARD_URI:http://localhost:8085}
          predicates:
            - Path=/api/alarm/**
          filters:
            - StripPrefix=1
        - id: mes-ai
          uri: ${MES_AI_URI:http://localhost:8087}
          predicates:
            - Path=/api/ai/**
          filters:
            - StripPrefix=2
        - id: mes-dashboard-ws
          uri: ${MES_DASHBOARD_WS_URI:ws://localhost:8085}
          predicates:
            - Path=/api/ws/**
          filters:
            - StripPrefix=1
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOriginPatterns: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true
            maxAge: 3600

jwt:
  secret: ${JWT_SECRET:}
  expiration: ${JWT_EXPIRATION:86400000}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized
