# Virtual Path MES - AI 服务运行时依赖
# 仅含 src/ 实际 import 的包，无 torch/CUDA：
#   RAG 知识库 = TF-IDF（numpy/scikit-learn）
#   模型推理 = LightGBM / XGBoost / ONNX Runtime（CPU）
fastapi==0.115.6
uvicorn[standard]==0.34.0
lightgbm==4.5.0
xgboost==2.1.1
onnxruntime==1.19.2
scikit-learn>=1.5
numpy>=1.26,<3.0
pandas==2.2.3
pyyaml==6.0.2
redis==5.2.1
httpx==0.28.1
zhipuai>=2.1.5
pymysql>=1.1.0

# onnxruntime 兼容性约束（protobuf 5.x 不兼容）
protobuf>=3.20,<5

# 单元测试
pytest==8.3.4

# 训练并导出 ONNX 时额外安装（train_models.py 为可选导入，缺失时自动 fallback 保存 pkl）：
#   pip install onnxmltools onnx
