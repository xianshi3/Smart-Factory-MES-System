server:
  host: "0.0.0.0"
  port: 8087

database:
  host: "localhost"
  port: 3306
  username: "root"
  password: "123455"
  database: "mes_db"

model:
  quality:
    onnx_path: "src/models/saved_models/quality_predict.onnx"
    version: "1.0.0"
  production:
    onnx_path: "src/models/saved_models/output_predict.onnx"
    version: "1.0.0"

llm:
  # 密钥只从环境变量 ZHIPU_API_KEY 读取（此文件中的占位不会生效）
  api_key: ""
  model: "glm-4-flash"
  rate_limit: 60

redis:
  host: "localhost"
  port: 6379
  db: 1

feature:
  sliding_window_size: 60
  aggregation_interval: 10
